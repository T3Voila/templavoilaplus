pull_request_rules:
  - name: Conventional Commit
    conditions:
      - "base=main"
      - "#approved-reviews-by>=1"
      - "#changes-requested-reviews-by=0"
    actions:
      merge:
        method: merge
